# Abaqus job artifacts (regenerated by every run of a scripts/*.py model-building script)
*.odb
*.odb_f
*.log
*.msg
*.sta
*.prt
*.com
*.dat
*.inp
*.env
*.lck
*.rpy
*.rpy.*
*.rec
*.SMABulk
*.res
*.stt
*.mdl
*.023

# VS Code workspace settings
.vscode/

# Claude's notes and settings
CLAUDE.md
.claude/

# Markdown notes
_sources.md
abqpy_notes.md

# Results files written by scripts/*.py for notebooks to read back in -- regenerated output,
# not source.
scripts/*.json

# Every Monte Carlo campaign's output -- per-run full history JSON, summary CSV, and timing
# JSON -- written under its own notebooks/<nb>-numbered subfolder (runs/nb02/, runs/nb03/,
# runs/nb04/) by that notebook (or its standalone background driver script). Regenerated
# output, not source; kept out of scripts/ so results never mix with code.
runs/

# Python bytecode cache directories
scripts/__pycache__/

# Literature sources
sources/